// Preview mode permits read-only API fallback. Live mode never substitutes demo data.
window.TECHPRO_CONFIG = Object.freeze({
  mode: 'demo',
  demoApiReads: false,
  apiBase: 'api/',
  requestTimeoutMs: 5000,
  adminWhatsApp: ''
});
